#pragma once
/*
 * rexc_vector  –  Compatibility header mapping std::vector to rexc_rt::rexc_vector
 */
#include "../runtime/rexc_containers.hpp"

namespace std {
    template <typename T>
    using vector = rexc_rt::rexc_vector<T>;
}
